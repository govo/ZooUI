import ChannelSelector from './ChannelSelector.vue'
/* istanbul ignore next */
ChannelSelector.install = function(Vue) {
  Vue.component(ChannelSelector.name, ChannelSelector);
};

export default ChannelSelector